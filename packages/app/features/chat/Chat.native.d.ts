type ChatScreenProps = {
    conversationId: string;
    name: string;
};
export default function ChatScreen({ conversationId, name }: ChatScreenProps): import("react/jsx-runtime").JSX.Element;
export {};
